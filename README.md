# Northstar Personal Site

A lightweight static personal website built with plain HTML, CSS, and JavaScript.

## Local Preview

Open `index.html` directly, or serve the folder with:

```sh
python3 -m http.server 4173
```

